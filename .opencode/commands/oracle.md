---
description: v2.0.0 L-CMD | Manage Oracle skills and profiles. Use when user says "oracle", "profile", "install skill", "remove skill", "switch profile".
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /oracle

Execute the `oracle` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "oracle"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/oracle/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
