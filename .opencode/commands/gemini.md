---
description: v2.0.0 L-CMD | Control Gemini via MQTT WebSocket. Use when user says "gemini", needs to send messages to Gemini, or control the Gemini browser tab.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /gemini

Execute the `gemini` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "gemini"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/gemini/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
