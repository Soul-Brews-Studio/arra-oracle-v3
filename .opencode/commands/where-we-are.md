---
description: v2.0.0 L-CMD | Session awareness - alias for /recap --now. Use when user asks "now", "where are we", "what are we doing", "status".
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /where-we-are

Execute the `where-we-are` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "where-we-are"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/where-we-are/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
