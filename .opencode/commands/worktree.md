---
description: v2.0.0 L-CMD | Git worktree for parallel work. Use when user says "worktree", "parallel work", "new agent", "start parallel".
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /worktree

Execute the `worktree` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "worktree"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/worktree/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
