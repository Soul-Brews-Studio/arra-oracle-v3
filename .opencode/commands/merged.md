---
description: v2.0.0 L-CMD | Post-Merge Cleanup. Switch to main, pull latest, delete merged branch. Use after PR is merged.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /merged

Execute the `merged` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "merged"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/merged/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
