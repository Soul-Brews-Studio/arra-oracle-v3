---
description: v2.0.0 L-CMD | Talk to an agent via Oracle threads. Use when user says "talk to", "message", "chat with", or wants to communicate with another agent.
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /talk-to

Execute the `talk-to` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "talk-to"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/talk-to/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
