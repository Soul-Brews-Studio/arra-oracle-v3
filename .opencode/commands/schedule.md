---
description: v2.0.0 L-CMD | Query schedule via Oracle API (Drizzle DB). Use when user says "schedule", "upcoming events", "what's on today", "calendar".
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /schedule

Execute the `schedule` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "schedule"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/schedule/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
