---
description: v2.0.0 L-CMD | Explore a codebase with parallel Haiku agents. Modes - --fast (1 agent), default (3), --deep (5). Use when user says "learn [repo]", "explore codebase", "study this repo".
allowed-tools:
  - Bash
  - Read
  - Write
  - Edit
  - Glob
  - Grep
  - Task
  - WebFetch
---

# /learn

Execute the `learn` skill with args: `$ARGUMENTS`

**If you have a Skill tool available**: Use it directly with `skill: "learn"` instead of reading the file manually.

**Otherwise**: Read the skill file at `/Users/non/dev/arra-oracle-v3/.opencode/skills/learn/SKILL.md` and follow ALL instructions in it.

---
*oracle-skills-cli v2.0.0*
